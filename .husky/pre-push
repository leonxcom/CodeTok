#!/bin/bash
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 运行代码检查和构建测试..."
pnpm lint && pnpm build:test

# 构建测试失败时退出
if [ $? -ne 0 ]; then
  echo "❌ 构建测试失败，请修复错误后再次推送！"
  exit 1
fi

echo "✅ 构建测试通过！" 